/*
 ============================================================================
 Name        : Modulo.c
 Author      : 
 Version     :
 Copyright   : Your copyright notice
 Description : Hello World in C, Ansi-style
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>
#include <commons/config.h>
#include <commons/collections/list.c>
#include <commons/temporal.h>

FILE* enlistarArchivo(rutaDeArchivo){
	FILE* archivo = fopen(rutaDeArchivo,"w+b");
	t_list lista = list_create();
	char* linea = malloc(sizeof());
	fread(linea)
	while(!feof(archivo)){

		fread(lista->,sizeof())
	}
}

int Modulo(int argc, char*argv[]) {

	FILE* archivo = fopen(*argv,"r");

	char* buffer = malloc();
	char
	while(!feof(archivo)){
		fread()

	}
	
	while()
	archivoParseado = enlistarArchivo(**argv);
	


}
